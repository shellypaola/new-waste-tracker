import React from 'react';

// Minimal test component - no fancy features
export default function RewardsScreenTest({ colors }) {
  return (
    <div className="h-full overflow-y-auto pb-24 px-4 pt-6">
      <h1 className="text-2xl font-bold mb-6" style={{ color: colors.text }}>
        Rewards Test
      </h1>
      
      <div className="bg-white p-6 rounded-2xl mb-6">
        <p className="text-lg mb-2">If you see this, the component loads!</p>
        <p className="text-sm" style={{ color: colors.textSecondary }}>
          The error is likely in the full component code.
        </p>
      </div>

      <div className="bg-white p-4 rounded-2xl">
        <p className="font-bold mb-2">Color Test:</p>
        <div style={{ color: colors.text }}>Text color works</div>
        <div style={{ color: colors.primary }}>Primary color works</div>
        <div style={{ color: colors.secondary }}>Secondary color works</div>
      </div>
    </div>
  );
}
